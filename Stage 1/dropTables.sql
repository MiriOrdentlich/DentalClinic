-- Drop the Payment table first
DROP TABLE Payment;
-- Documentation: The Payment table is dropped first as per the specified order.

-- Drop the MUsedInT table next
DROP TABLE MUsedInT;
-- Documentation: The MUsedInT table is dropped next as per the specified order.

-- Drop the PreformedIn table
DROP TABLE Tpreformedina;
-- Documentation: The PreformedIn table is dropped as per the specified order.

-- Drop the OMakeA table
DROP TABLE OMakeA;
-- Documentation: The OMakeA table is dropped as per the specified order.

-- Drop the Treatment table
DROP TABLE Treatment;
-- Documentation: The Treatment table is dropped as per the specified order.

-- Drop the Material table
DROP TABLE Material;
-- Documentation: The Material table is dropped as per the specified order.

-- Drop the Appointment table
DROP TABLE Appointment;
-- Documentation: The Appointment table is dropped as per the specified order.

-- Drop the Patient table
DROP TABLE Patient;
-- Documentation: The Patient table is dropped as per the specified order.

-- Drop the Office table
DROP TABLE Office;
-- Documentation: The Office table is dropped as per the specified order.

-- Drop the Doctor table
DROP TABLE Doctor;
-- Documentation: The Doctor table is dropped as per the specified order.

-- Drop the Staff table last
DROP TABLE Staff;
-- Documentation: The Staff table is dropped last as per the specified order.
